name: Rakit AmneziaWG Flint
on:
  workflow_dispatch:

jobs:
  build:
    name: Kompilasi Paket IPK AmneziaWG
    runs-on: ubuntu-22.04
    steps:
      - name: Ambil Kode Sumber
        uses: actions/checkout@v4

      - name: Siapkan Lingkungan Ubuntu
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk gcc-multilib gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget

      - name: Unduh OpenWrt SDK Flint
        run: |
          # Mengunduh SDK khusus untuk arsitektur Flint IPQ6000 (aarch64_cortex-a53)
          wget https://archive.openwrt.org/releases/21.02.3/targets/ipq6000/generic/openwrt-sdk-21.02.3-ipq6000-generic_gcc-11.2.0_musl.Linux-x86_64.tar.xz
          tar -xf openwrt-sdk-*.tar.xz
          mv openwrt-sdk-* sdk

      - name: Masukkan Resep AmneziaWG
        run: |
          cd sdk
          echo "src-git amnezia https://github.com/amnezia-vpn/amneziawg-openwrt.git;main" >> feeds.conf.default
          ./scripts/feeds update amnezia
          ./scripts/feeds install -a -p amnezia
          
          # Mengatur konfigurasi agar modul ikut dikompilasi
          make defconfig
          echo "CONFIG_PACKAGE_kmod-amneziawg=m" >> .config
          echo "CONFIG_PACKAGE_amneziawg-tools=m" >> .config
          make defconfig

      - name: Proses Merakit Kompilasi
        run: |
          cd sdk
          # Memulai kompilasi khusus untuk paket AmneziaWG
          make package/amneziawg-tools/compile V=s

      - name: Simpan Hasil Jadi (Artifact)
        uses: actions/upload-artifact@v4
        with:
          name: AmneziaWG-Flint-IPK
          path: sdk/bin/targets/ipq6000/generic/packages/kmod-amneziawg*.ipk
